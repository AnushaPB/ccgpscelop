```{r}
library(terra)

# Create a matrix with a vertical gradient
mat <- matrix(rep(0:2, each = 100 * 100 / 3), nrow = 100, ncol = 100)

# Create a raster from the matrix
r <- rast(mat)

# Plot the raster
plot(r)
```